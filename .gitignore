node_modules/
output/

# テスト用画像。今後ここに実際のゲームスクリーンショットを置くようになった際、
# 誤ってpushしないよう画像自体は追跡対象から外す（規約を書いたREADMEだけは残す）。
test-images/*
!test-images/README.md

# 検証素材（ゲームのスクリーンショット・画面収録）の置き場所を書いたローカル設定。
# パスをリポジトリに直書きしないための仕組みなので、これ自体は追跡しない。
.skillset-assets.json

# 受け取った画面収録の一時置き場（素材はリポジトリ外のフォルダで管理する）。
videos/
